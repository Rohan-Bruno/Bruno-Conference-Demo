info:
  name: List Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
