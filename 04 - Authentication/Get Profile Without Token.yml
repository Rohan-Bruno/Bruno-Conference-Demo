info:
  name: Get Profile Without Token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/me"

runtime:
  scripts:
    - type: tests
      code: |-
        test("rejected without a token", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
