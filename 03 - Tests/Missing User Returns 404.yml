info:
  name: Missing User Returns 404
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/999"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 404 for unknown user", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
